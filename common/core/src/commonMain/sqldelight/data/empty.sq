CREATE TABLE test(
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    value TEXT
);

insert:
INSERT INTO test(value)
VALUES (?);

select:
SELECT * FROM test;

update:
INSERT OR REPLACE INTO test(value)
VALUES (?);
